import {StyledLayout} from "../layout/styled-layout";

export const Home = () => {
    const title="Chat";
    return (
        <StyledLayout title={title}>
            Home
        </StyledLayout>
    );
}
